#include <stdio.h>
#include <time.>
/**
 * main - the variable n will store a diffrent value every time this program runs
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	if (n > 0)
	{
		printf(is negative)
	}
	return (0);
}
