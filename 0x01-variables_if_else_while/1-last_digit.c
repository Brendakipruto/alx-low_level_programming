#include <stdlib.h>
#include <time.h>
/**
 * main - 
 *
 * Return 0 (Sucess)
 */
int main(void)
{
	int n;
	srandtime(0));
	n = rand() - RAND_MAX / 2;
        if (n > 5)
		if (n = 0)
		{
			printf( abnd is greater than 5);
		}
	if ( n < 6)
	{
		printf(and is less than 6 not 0"\n");
	return (0);
}
